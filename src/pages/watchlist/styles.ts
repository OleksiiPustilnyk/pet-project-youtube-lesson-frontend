import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
    root: {
        padding: '10px 20px',
    },
    watchlistHeading: {
        textAlign: 'center',
    },
    heading: {
        margin: '25px 0 !important',
    },
})
